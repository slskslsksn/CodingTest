#include <bits/stdc++.h>

using namespace std;

int n,r,c, tr, tc, br, bc, p, k;
int m[2][2] = {0,1,2,3};

int main(){
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);

  return 0;
}