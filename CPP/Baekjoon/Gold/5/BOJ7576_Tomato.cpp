#include <bits/stdc++.h>

using namespace std;

int m, n, cnt;
int dy[] = {1, 0, -1, 0};
int dx[] = {0, 1, 0, -1};
bool v[1000][1000];

int main(){
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);

  cin >> m >> n;
  const int M = m, N = n;
  bool v[M][N], s[M][N];
  fill(v[0][0], v[M][N+1], 0);

  return 0;
}