#include <bits/stdc++.h>

using namespace std;

string a, b, ret;
int c;

int main(){
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);

  cin >> a >> b;
  cout << max(a, b);

  return 0;
}